# Database Configuration
# Option 1: Local PostgreSQL (requires PostgreSQL installed)
# DATABASE_URL=postgresql://username:password@localhost:5432/stratosys_db

# Option 2: Supabase (recommended for development)
# Get your connection string from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/database
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Option 3: Docker PostgreSQL (if using docker-compose)
DATABASE_URL=postgresql://postgres:password@localhost:5432/stratosys_db

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-jwt-key-change-this-in-production

# Server Configuration
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:3002

# Email Configuration (for password reset, etc.)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
