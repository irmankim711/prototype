<!DOCTYPE html>
<html>
<head>
    <title>Automated Form Data Report</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .header { background-color: #f0f0f0; padding: 20px; border-radius: 5px; }
        .summary { margin: 20px 0; }
        .data-table { width: 100%; border-collapse: collapse; margin: 20px 0; }
        .data-table th, .data-table td { border: 1px solid #ddd; padding: 8px; text-align: left; }
        .data-table th { background-color: #f5f5f5; font-weight: bold; }
        .analytics { background-color: #e7f3ff; padding: 15px; border-radius: 5px; margin: 20px 0; }
    </style>
</head>
<body>
    <div class="header">
        <h1>Automated Form Data Report</h1>
        <p><strong>Generated:</strong> {{ generated_at }}</p>
        <p><strong>Total Records:</strong> {{ data|length - 1 if data else 0 }}</p>
    </div>

    <div class="summary">
        <h2>Data Summary</h2>
        {% if data and data|length > 1 %}
            <p>This report contains {{ data|length - 1 }} submission(s) collected from the automated form system.</p>
        {% else %}
            <p>No data available in this report.</p>
        {% endif %}
    </div>

    {% if data and data|length > 1 %}
    <div class="data-section">
        <h2>Collected Data</h2>
        <table class="data-table">
            <thead>
                <tr>
                    {% for header in data[0] %}
                    <th>{{ header }}</th>
                    {% endfor %}
                </tr>
            </thead>
            <tbody>
                {% for row in data[1:] %}
                <tr>
                    {% for cell in row %}
                    <td>{{ cell if cell is not none else '' }}</td>
                    {% endfor %}
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <div class="analytics">
        <h2>Analytics</h2>
        <ul>
            <li><strong>Total Submissions:</strong> {{ data|length - 1 }}</li>
            <li><strong>Report Generated:</strong> {{ generated_at }}</li>
            <li><strong>Data Source:</strong> Automated Form Collection System</li>
        </ul>
    </div>
    {% endif %}

    <div class="footer" style="margin-top: 40px; padding-top: 20px; border-top: 1px solid #ddd;">
        <p><small>This report was automatically generated by the Form Automation System</small></p>
    </div>
</body>
</html>
